# Data

Local datasets are not committed to this repository.

- Put original downloaded files in `data/raw/`.
- Put cleaned or converted files in `data/processed/`.
- Keep only small examples or placeholder `.gitkeep` files in git.

The `.gitignore` keeps dataset contents out of commits by default.
